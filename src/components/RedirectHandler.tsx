/**
 * RedirectHandler 
 * Client-side redirects have been disabled to prevent Google Search Console "Redirect Errors".
 * All URL normalization (www to non-www, trailing slashes) is now handled server-side via Cloudflare.
 */
const RedirectHandler = () => {
  return null;
};

export default RedirectHandler;

